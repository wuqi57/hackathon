/**
 * @file 
 * @author wuqi03(wuqi03@baidu.com)
 */

define(function (require) {

    var config = {};

    config.view = require('./infoView');

    config.model = require('./infoModel');

    return config;

});
